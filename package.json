{
  "name": "XMLProject",
  "version": "0.0.1",
  "scripts": {
    "setup": "cd api && npm install && npm run create-db && npm run migrate && npm run seed && cd ../website && npm install",
    "start": "start cmd /k \"cd api && npm run start\" & start cmd /k \"cd website && npm run dev\""
  }
}
